# Installed into the SageMaker TensorFlow training container on top of its
# bundled tensorflow/numpy. These are the non-TF libraries the pipeline imports:
# Pillow (crop I/O), scikit-learn (metrics), matplotlib (confusion figures).
Pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
